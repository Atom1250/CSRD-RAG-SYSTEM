# Grafana dashboard provisioning

apiVersion: 1

providers:
  - name: 'CSRD RAG System'
    orgId: 1
    folder: 'CSRD RAG'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards